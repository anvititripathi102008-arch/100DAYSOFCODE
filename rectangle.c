#include<stdio.h>
int main()
{
printf("let the length of the rectangle be l and width be w");
int l =14;
int w =12;
int area =l*w;
int perimeter =2*(l+w);
printf("\n the area of the rectangle be %d", area);
printf("\n the perimeter of the rectangle be %d", perimeter);
return 0;
}
